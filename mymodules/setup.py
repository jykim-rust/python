from setuptools import setup

setup(
    name='vsearch',
    version='1.1',
    description='the head firtst python search tools',
    author='jykim',
    py_modules=['vsearch'],
)

lista=[1,2,3]
def double(arg):
    arg=arg*2


def change(arg):
    arg.append('new')

